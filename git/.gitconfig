[core]
	editor = nvim
	ignoreCase = false
	pager = delta
[interactive]
	diffFilter = delta --color-only
[include]
	path = catppuccin.gitconfig
[delta]
	navigate = true
	; Default theme. Overridden by delta.features in ~/.gitconfig.local
	; (macOS dark-notify) and by the DELTA_FEATURES env var (SSH THEME_MODE).
	features = catppuccin-mocha
[merge]
	conflictstyle = diff3
[diff]
	colorMoved = default
[alias]
	lg = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s'
	sf = show --pretty=format: --name-only
	pp = !sh -c 'git pull && git push'
	ll = log --pretty=format:%C(yellow)%h%Cred%d\\ %Creset%s%Cblue\\ [%cn] --decora$
	br = branch -avv
	s = status
	ac = !git add . -A && git commit
	p = pull
	pu = push
	d = diff
	co = checkout
[push]
	default = current
[init]
	defaultBranch = main
[credential "https://github.com"]
	helper =
	helper = !gh auth git-credential
[credential "https://gist.github.com"]
	helper =
	helper = !gh auth git-credential
[filter "lfs"]
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
[gc]
	autoDetach = false
